enum EventStatus {
  CREATED = "created",
  INVITED = "invited",
  ASSIGNED = "assigned",
  DONE = "done",
}

enum EventMode {
  CLASSIC = "classic",
  SCRAP = "scrap",
  CUSTOM = "custom",
}

enum DrawRule {
  CHAIN = "chain",
  EXCHANGE = "exchange",
  PICK_ORDER = "pick-order",
}

enum InviteStatus {
  INVITED = "invited",
  OPENED = "opened",
  ACCEPTED = "accepted",
  DENIED = "denied",
}
